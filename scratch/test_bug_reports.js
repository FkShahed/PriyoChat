const axios = require('axios');
const API = 'https://priyochat.onrender.com/api';

async function test() {
  try {
    // We don't know the admin password easily, but we can look at the database or bypass
    // Actually, I can just use a local connection to the DB if I need to, but the remote DB is MongoDB Atlas.
    console.log("We need to see the actual error");
  } catch (e) {
    console.error(e);
  }
}
test();
